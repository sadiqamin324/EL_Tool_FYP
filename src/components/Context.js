import { createContext, useContext } from "react";

export const Title = createContext();

export const DataContext = createContext();

export const SourceDestinationflag = createContext();

export const SourceTables = createContext();

export const DestTables = createContext();

export const Password = createContext();

export const SourceColumns = createContext();

export const DialogueContext = createContext();
export const isHidden = createContext();
